import Modal from "@/components/style-components/modal";

const FormOrder = ({isOpen,onClose,data}) => {
    return (
        <Modal isOpen={isOpen} onClose={onClose} title={'Form Order'}>
            <div>
                AAAA
            </div>
        </Modal>
    )
}

export default FormOrder;