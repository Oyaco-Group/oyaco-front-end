import Modal from "@/components/style-components/modal";

const DetailOrder = ({isOpen, onClose}) => {
    return (
         <Modal isOpen={isOpen} onClose={onClose} title={"Detail Order"}>
            
         </Modal>

    )
}

export default DetailOrder;